{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Chrome for HTML/CSS",
            "file": "${workspaceFolder}/index.html"
        }
    ]
}
